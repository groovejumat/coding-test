https://programmers.co.kr/learn/courses/30/lessons/42587?language=python3

## my solution
```python
# 1. locationarr가 완성되어지는 시점에 while문 작업이 끝나도록 하기 현재 싸이클이 더 돌고 있는 중
# 2. 해당 arr에 있는 index값을 참조하여 값을 가지고 올 수 있도록하기
def solution(priorities, location):
    priormax = max(priorities)
    locationarr = []
    firstpriorcnt=len(priorities)

    print(priorities)
    print(len(priorities))
    # for i in range(priorities):
    #     print(i)
    i = 0
    
    #while len(priorities) > 1 :
    while firstpriorcnt > len(locationarr) : 
        i += 1
        # 지속적으로 검증 
        for index, value in enumerate(priorities):
            print("인덱스 값 ", index)
            if priormax == value : 
                print("제일 큰 값을 빼냅니다.")
                print(index, value)
                priorities[index]=0
                locationarr.append(index)
                if len(priorities) > 0 :
                    priormax = max(priorities)
                    print("새로운 max값을 할당했습니다", priormax)
            print("값")
            print(priorities)
    print(locationarr)
    answer=0
    return answer
```

## most like solution
```python

```